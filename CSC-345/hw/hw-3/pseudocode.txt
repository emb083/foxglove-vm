main creates threads with run *philosopher

*philosopher:
    while (eatcount < numEat)
        if h
            test()
            if can eat:
                pickupfork <- sem_wait
                set to e
        if e
            wait
            putdownfork <- sem_post
            set to t
        if t
            wait
            set to h


join threads
exit
